
function findBox(freezers,box_ID){

  var freezers = freezers;
  var box_ID = box_ID;
  var location = {};


  for (var i = 0; i < freezers.length; i++){

      //freezers[i]

      for(var j = 0; j < freezers[i].shelves.length; j++){

        //freezers[i].shelves[j]

          for(var k = 0; k < freezers[i].shelves[j].length; k++){

            //freezers[i].shelves]j].racks[k]

            for(var l = 0; l < freezers[i].shelves[j].racks[k].length; k++){

              for(var m = 0; m < freezers[i1.shelves[j].racks[k].rows[l].length; m++){

                if(JSON.stringify(freezers[i1.shelves[j].racks[k].rows[l].columns[m].box.box_ID) === box_ID){

                  location.freezer = freezers[i].freezername;
                  location.shelf = freezers[i].shelves[j].shelf_name;
                  location.rack = freezers[i].shelves[j].racks[k].rack_name
                  location.row = freezers[i].shelves[j].racks[k].rows[l].row_name
                  location.column = freezers[i].shelves[j].racks[k].rows[l].columns[m].column_name


                  return location
                };

              };

            };

          };

      };

  };
























}
